import { Landing } from '@components/landing/Landing';

export default function Home() {
  return (
    <div id={'home-page'} className={'min-h-[100svh] min-w-[100%]'}>
      <Landing />
    </div>
  );
}
