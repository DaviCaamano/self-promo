'use client'
import { Selfie } from '@components/landing/selfie/Selfie';

export const Landing = () => {
  return (
    <div id={'Landing'} className={'min-h-screen min-w-screen flex justify-center items-center'}>
      <Selfie />
    </div>
  );
};
